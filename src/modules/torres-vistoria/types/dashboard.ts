/** Indicadores consolidados do painel de vistorias. */
export interface DashboardMetrics {
  totalInspections: number;
  totalRevenue: number;
  netProfit: number;
  averageTicket: number;
  pendingInspections: number;
  completedInspections: number;
}
