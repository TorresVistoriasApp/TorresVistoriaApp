# Copie para supabase/.env.local (gitignored) se preferir arquivo em vez do wrapper npm.
#
# O CLI exige este valor para validar [auth.hook.send_email] em config.toml
# ao rodar db push / db reset — isso NÃO envia auth config ao remoto.
#
# Produção: o secret real fica no Dashboard (Authentication → Hooks → Send Email).
# Edge Function send-email: supabase secrets set SEND_EMAIL_HOOK_SECRET=...
#
SEND_EMAIL_HOOK_SECRET=v1,whsec_bG9jYWxDbGlab25seVZhbGlkYXRpb25QbGFjZWhvbGRlcg==
